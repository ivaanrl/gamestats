import { getTopStreams, getTopStreamsCompletedAction } from "./games";
import { addGameInfo } from "./gameInfo";

const allActions = {
  getTopStreams,
  getTopStreamsCompletedAction,
  addGameInfo,
};

export default allActions;
